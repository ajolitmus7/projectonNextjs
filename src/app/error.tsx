"use client";
import React from "react";

const error = () => {
  const design = { width: "80%", display: "flex", justifyContent: "center" };
  return (
    <div style={design}>
      <h2>ERROR FOUND</h2>
    </div>
  );
};

export default error;
